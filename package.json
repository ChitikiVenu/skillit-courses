{
  "name": "skillit-courses",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "npm run sitemap && vite build",
    "preview": "vite preview",
    "sitemap": "node scripts/generate-sitemap.mjs",
    "seo:audit": "npm run sitemap --silent && node scripts/seo-audit.mjs"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-helmet-async": "^3.0.0",
    "react-router-dom": "7.18.3"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.5",
    "vite": "^8.2.0"
  }
}
