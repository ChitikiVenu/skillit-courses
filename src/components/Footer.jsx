// Deliberately empty per spec — a placeholder boundary at the bottom of every page, no content
// yet. Add real footer content (links, contact info, socials) here when it's ready.
export default function Footer() {
  return <footer className="site-footer" />;
}
