// Deliberately empty: the WhatsApp and booking buttons float on screen (FloatingActions), so the
// footer is just a boundary at the bottom of every page.
export default function Footer() {
  return <footer className="site-footer" />;
}
