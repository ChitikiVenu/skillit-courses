# The site's canonical public URL — every canonical tag, Open Graph/Twitter URL, JSON-LD block,
# sitemap entry and robots.txt Sitemap: line is built from this one value (src/config/site.js).
#
# When a custom domain is connected later, change this one line (here for local dev, and in the
# Environment Variables settings of the Vercel project for production) — no code changes needed.
VITE_SITE_URL=https://www.skillit.education

# Optional: GA4 measurement ID (e.g. G-XXXXXXXXXX). Leave unset and no analytics script is loaded;
# set it (here and in the Vercel project's Environment Variables) to turn on the events in
# src/utils/analytics.js: page_view, form_start, form_submit, click_to_call, whatsapp_click, brochure_download.
VITE_GA_ID=

# Enquiry forms -> Google Sheet + email to info@skilliteducation.com (see docs/lead-capture-setup.md).
# The Apps Script web app URL, and the same random token you put in scripts/google-apps-script/leads.gs.
VITE_LEADS_ENDPOINT=
VITE_LEADS_TOKEN=
